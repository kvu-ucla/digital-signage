import type { ScreenProps } from "../../lib/resolveScreen";
import { RendezvousTemplate } from "../template";

export default function WestByo({ data }: ScreenProps) {
  return <RendezvousTemplate data={data} />;
}
